{
  "name": "software-project-tracker",
  "version": "0.1.0",
  "private": true,
  "description": "Tooling baseline for FRD-driven implementation.",
  "engines": {
    "node": "22.11.x",
    "npm": ">=10.0.0"
  },
  "scripts": {
    "build": "tsc --noEmit -p tsconfig.json",
    "prompt:list": "node scripts/assemble-prompts.mjs --list",
    "prompt:lint": "node scripts/assemble-prompts.mjs --lint --all",
    "prompt:assemble": "node scripts/assemble-prompts.mjs --all",
    "test": "npm run test:unit",
    "test:unit": "vitest run --config vitest.config.ts --passWithNoTests --dir src/domain && vitest run --config vitest.config.ts --passWithNoTests --dir tests/unit",
    "test:integration": "vitest run --config vitest.config.ts --passWithNoTests --dir tests/integration",
    "test:e2e": "playwright test --config playwright.config.ts --pass-with-no-tests",
    "test:e2e:install": "playwright install chromium",
    "profile:review-run": "node scripts/profile-review-run.mjs --iterations 5 --out .codex/pr/profile-review-run.json",
    "validate:schema": "ajv validate -c ajv-formats --spec=draft2020 --strict=true -s specs/frds/frd-schema.json -d \"specs/frds/frd-[0-9][0-9][0-9]-*.json\"",
    "dev": "vite",
    "build:app": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "@testing-library/dom": "^10.0.0",
    "@testing-library/jest-dom": "^6.0.0",
    "@testing-library/user-event": "^14.0.0",
    "@types/react": "^18.3.28",
    "@types/react-dom": "^18.3.7",
    "@vitejs/plugin-react": "^4.7.0",
    "ajv-cli": "5.0.0",
    "autoprefixer": "^10.4.20",
    "jsdom": "^24.0.0",
    "postcss": "^8.4.49",
    "tailwindcss": "^3.4.17",
    "typescript": "^5.6.3",
    "vite": "^5.4.21",
    "vitest": "^1.6.0"
  }
}
